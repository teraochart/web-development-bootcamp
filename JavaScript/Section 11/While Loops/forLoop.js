/*for(init; condition; step) {
    run some code
}*/

//Initialize = Declare variable only exists inside the for loop
//Condition = The "while this is true"
//Step = What to do at the end of the loop (add 1 etc)

for(var count = 0; count < 6; count++) {
    console.log(count);
}

var str = "hello";

for( i = 0; i < str.length; i++) {
    console.log(str[i]);
}

